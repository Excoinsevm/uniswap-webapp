# Development version uses locally running Hardhat network
# Other environment variables are set in the autogenerated .local override file
REACT_APP_NETWORK_CHAIN_ID=31337

# Parsed as JSON and passed to all write calls
REACT_APP_TX_OVERRIDES={ "gasLimit": 30000000 }